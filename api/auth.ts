// MUST HAVE
const register = ({
  email,
  password,
  phone,
  firstName,
  lastName
}) => {}; // USER POST /api/auth/register
const login = ({
  email,
  password
}) => {}; // USER POST /api/auth/login

// PERCHANCE?
const updatePassword = ({oldPassword, newPassword}) => { }; // USER PUT /api/auth/password






























